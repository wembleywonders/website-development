import { CreatorFactoryDashboard } from '../../components/creator-factory';

/**
 * Admin Dashboard - Creator Factory Metrics
 * Shows mission impact, pipeline stages, revenue splits, and goals
 */
export function CreatorMetricsDashboard() {
  return <CreatorFactoryDashboard />;
}

export default CreatorMetricsDashboard;
