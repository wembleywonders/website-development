// Mount Stupid module — shared types.
// 'bright' = Bright Sparks taster track (plain language, no formula literacy assumed).
// 'stem'   = STEMgeneers track (explicit tie-in to the STEM-APM unit).
export type Track = 'bright' | 'stem';
