# Provisional Transaction Data Extract — 2026-08-23 — For Blake's Review, Not Authoritative

**This is raw extracted data intended only to support Blake’s (ACCA-qualified voluntary advisor) future draft accounts work. It is not accounts, not a disclosure, and not statutory material. Every figure below requires Blake’s review before it is used in anything filed. This document contains numbers and their sources only — no accounting judgement or disclosure treatment has been applied.**

Generated by `scripts/finance/extract-transaction-data.mjs`, run 2026-08-23T11:02:26.954Z.
Compared against the prior run dated 2026-08-23.

## Changed since the last run

No changes detected in any tracked check since the prior run.

## All checks, this run

| Check | Status | Detail | Source |
|---|---|---|---|
| ATELIER_COMMISSION backend settlement flag | OFF | Confirmed off — directors’ decision, per the code’s own comment. Settlement not automated; no real ATELIER_COMMISSION transactions can exist while this is off. | `src/blockchain/config/revenueModels.ts (header comment)` |
| STANDARD split (config only, not a transaction total) | CONFIG-CONFIRMED | maker 55% / platform 25% / community 20% | `src/blockchain/config/revenueModels.ts` |
| ATELIER_COMMISSION split (config only, not a transaction total) | CONFIG-CONFIRMED | maker 75% / platform 20% / community 5% | `src/blockchain/config/revenueModels.ts` |
| ATELIER_AUCTION split (config only, not a transaction total) | CONFIG-CONFIRMED | maker 70% / platform 20% / community 10% | `src/blockchain/config/revenueModels.ts` |
| CommunityShopPage cart/checkout reality | CONFIRMED-NOT-REAL | Page’s own comment still confirms: "Add to basket" is local UI state only, no persistence, no real cart/order/checkout, no backend connection. | `src/pages/CommunityShopPage.tsx (header comment)` |
| CommunityShopPage routing | ROUTED-LIVE | Routed at both /shop and /cyberstore in src/App.tsx. | `src/App.tsx` |
| CyberstoreStorefront listing data | CONFIRMED-EMPTY-MOCK | fetchProductsMock() still hardcoded to return an empty array — zero real listings, by design, unchanged since 23 Aug 2026. | `src/studio/CyberstoreStorefront.tsx` |
| CyberstoreStorefront routing | STILL-NOT-ROUTED | No reference to CyberstoreStorefront in src/App.tsx — still unreachable via any live route, unchanged. | `src/App.tsx` |
| CyberstoreListingWizard submit behaviour | CONFIRMED-SIMULATED | handleSubmit() still a simulated delay + fake id, no real API call — nothing it "submits" is stored anywhere. Unchanged since 23 Aug 2026. | `src/production-hub/CyberstoreListingWizard.tsx` |
| CyberstoreListingWizard routing | STILL-NOT-ROUTED | No reference in src/App.tsx — still unreachable via any live route, unchanged. | `src/App.tsx` |
| Pardner reserve snapshot | CONFIRMED-STUBBED-HARDCODED | Still a hardcoded literal with isEstimate: true. Values: totalReserveGBP=4200, pardnerAllocationGBP=630, committedThisQuarterGBP=175, availableForNewPaymentsGBP=455, estimatedCreatorsEligibleThisQuarter=5. THESE ARE NOT REAL FIGURES — fictional placeholders, not usable in any form. | `src/features/pardner/CultivationPardnerTab.tsx (setReserve literal)` |
| Pardner individual record endpoint (/api/pardner/record/{id}) | CODE-CALLS-REAL-ENDPOINT-LIVENESS-UNKNOWN | This script can only confirm the frontend code shape (a genuine fetch() call exists), not whether a live backend actually answers it or what it returns. That requires checking outside this repository — cannot be automated from here. _(UNKNOWN — not verifiable from static source alone, every run.)_ | `src/features/pardner/CultivationPardnerTab.tsx` |
| src/marketplace (Order type / MarketplaceMayaROV) reachability | CONFIRMED-UNREACHABLE | Zero files outside src/marketplace/ import from it — still dead code, unchanged since 23 Aug 2026. | `full src/ import scan (this script)` |
| ww-accounts.md / ww-revenue-models.md existence | CONFIRMED-ABSENT | Neither file exists anywhere in the repo — unchanged since 23 Aug 2026. The ATELIER_COMMISSION flag check above is authoritative regardless (checked directly in code). | `full repo file scan (this script)` |

## Headline read (mechanical summary, not an opinion)

6 check(s) this run confirmed "no real transaction data" or "still stubbed/off/dead" in their prior known state. Any check above marked CHANGED, NOW-*, or *-CHECK-MANUALLY is where a human (Blake or engineering) should look first — those are the only rows where something may have moved since the baseline.

---

**Nothing in this document should be entered into any draft account, note, or working paper as a transaction figure without Blake’s review.** This script checks the Wembley Wonders frontend codebase only — it cannot query a live backend and does not attempt to.
